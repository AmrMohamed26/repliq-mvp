# Railway worker service — background BullMQ consumer (web app stays on Vercel).
[build]
buildCommand = "npm ci"

[deploy]
startCommand = "npm run worker"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
